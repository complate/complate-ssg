{
	"scripts": {
		"start": "npm-run-all --parallel compile-watch build-watch",
		"test": "eslint --cache --ext .js --ext .jsx *.js views && echo ✓",
		"build-watch": "nodemon -w dist/bundle.js -x 'node ./index.js'",
		"build": "node ./index.js",
		"compile-watch": "npm run compile -- --watch",
		"compile": "faucet --no-fingerprint --compact"
	},
	"dependencies": {
		"complate-ssg": "^0.9.0",
		"nodemon": "^1.11.0",
		"npm-run-all": "^4.0.2"
	}
}
