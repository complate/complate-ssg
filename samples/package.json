{
  "scripts": {
    "start": "npm-run-all --parallel compile-watch build-watch",
    "test": "eslint --cache --ext .js --ext .jsx *.js views && echo ✓",
    "build-watch": "nodemon -w dist/bundle.js -x 'node ./index.js'",
    "build": "./index.js",
    "compile-watch": "wrollup --config",
    "compile": "rollup --config"
  },
  "babel": {
    "presets": [
      "es2015-rollup"
    ],
    "plugins": [
      [
        "transform-react-jsx",
        {
          "pragma": "createElement"
        }
      ]
    ]
  },
  "dependencies": {
    "complate-ssg": "^0.8.0",
    "nodemon": "^1.11.0",
    "npm-run-all": "^4.0.2"
  }
}
